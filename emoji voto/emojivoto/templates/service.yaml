apiVersion: v1
kind: Service
metadata:
  name: {{ include "sampleproj.fullname" . }}
  labels:
    {{- include "sampleproj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sampleproj.selectorLabels" . | nindent 4 }}
