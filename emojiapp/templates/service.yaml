apiVersion: v1
kind: Service
metadata:
  name: {{ include "emojiapp.fullname" . }}
  labels:
    {{- include "emojiapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emojiapp.selectorLabels" . | nindent 4 }}
