apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "emojiapp.fullname" . }}-test-connection"
  labels:
    {{- include "emojiapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "emojiapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
